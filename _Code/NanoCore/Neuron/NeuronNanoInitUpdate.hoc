
proc reseedNeuronNanoBranches() {
    nanoBranchesManager.reseedBranches(createVector(isUniformOrNormalSpineGeometry))
}

proc UpdateSpines() {
    nanoBranchesManager.updateBranches(createVector(isUniformOrNormalSpineGeometry))
}
